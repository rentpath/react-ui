{
  "name": "react-ui-utils",
  "version": "0.0.1",
  "main": "lib/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf lib",
    "build": "babel --ignore node_modules -d lib src",
    "build:watch": "yarn run build -- --watch"
  },
  "dependencies": {
    "classnames": "^2.2.5"
  }
}
