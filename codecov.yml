# You may override any of these configurations in your own Team/Repository Yaml.
coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default: on
    patch:
      default: on
    changes:
      default: off
  # notify:
  #   slack:
  #     default:
  #       url: "https://hooks.slack.com/services/abc/123"
  #       threshold: 1%
  #       only_pulls: false
  #       branches: null
  #       flags: null
  #       paths: null
  # ci:
  #   - jenkins.domain.com       # add my custom jenkins server
# ignore:
#   - "path/to/folder"  # ignore folders and all its contents
#   - "test_*.rb"       # wildcards accepted
#   - "**/*.py"         # glob accepted
#   - "[a-z]+/test_.*"  # regexp accepted

comment:
  layout: "header, reach, diff, flags, files, footer"
  behavior: default
  require_changes: no
  require_base: no
  require_head: yes
